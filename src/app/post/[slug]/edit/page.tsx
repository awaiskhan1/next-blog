import { data } from "../../api";

const EditPost = ({ params }: { params: { slug: string } }) => {  
  return (
    <div>EditPost {params?.slug}</div>
    // filter data 
    // postField 
  )
}

export default EditPost